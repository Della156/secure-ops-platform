# Trae AI 设计提示词：模块6 — 运维配置中心

## 项目概况

这是一个**网络安全智能化运维平台**的前端项目，使用 Next.js + TailwindCSS + TypeScript 构建。
当前需要你开发 **模块6：运维配置中心** 下的所有三级页面。该模块的目标是：
> 建设平台自身的省级属地化配置与运维支撑功能，具备数据同步配置、运行参数配置、数据字典配置、数据供给配置与监控、访问行为配置及备份恢复配置等运维配置能力，实现底层规则、接口服务及属地运维数据的集中管控、无缝对接与安全控制，保障平台灵活、稳定、可维护运行。

## 你的任务

根据本文档中的功能描述，在已有项目中创建 **模块6** 的以下页面组件。
该模块共有 **13 个二级菜单**，每个对应一个目录和若干三级页面。
你需要：
1. 在 `src/pages/module{module_num}/` 目录下创建所有三级页面组件
2. 在 `src/data/menuData.ts` 的 `menu-6` 下补充三级菜单配置
3. 每个页面使用 'use client'，遵循深色主题设计规范

## 系统已有文件结构

```
src/
  data/menuData.ts              ← 菜单数据（含三级菜单结构）
  types/index.ts                ← MenuItem / SubMenuItem / ThirdMenuItem 类型
  components/
    Layout/MainLayout.tsx       ← 主布局
    Sidebar/Sidebar.tsx         ← 侧边栏容器
    Sidebar/MenuItem.tsx        ← 菜单项组件（支持三级展开）
    Pages/PageContent.tsx       ← 页面内容占位区域
    ui/Modal.tsx                ← 通用弹窗组件
    ui/Toast.tsx                ← 提示组件
    index.ts
  contexts/SystemContext.tsx    ← 系统上下文（含 activeMenu 状态）
  app/
    layout.tsx                  ← 根布局（含 SystemProvider）
    page.tsx                    ← 首页（PageContent）
    globals.css
```

## 菜单结构（模块6）

| 二级菜单 | 三级菜单列表 |
|---------|------------|
| 运维数据同步配置 | 数据源、目标与映射规则的同步任务配置、全量与增量同步策略及定时调度、同步任务状态监控与异常告警、同步日志审计 |
| 功能模块权限配置 | 通过建立组织架构树可视化展示与编辑机制、通过建立角色管理机制、通过建立功能菜单与操作按钮权限分配机制、通过建立数据范围权限控制机制、通过建立角色授权关系查询与审计机制 |
| 用户运行授权配置 | 通过建立用户账户信息配置机制、通过建立用户密码安全策略管理机制、通过建立用户账户状态管理机制、通过建立用户关联组织、角色及信息维护机制 |
| 运行参数配置 | 系统样式配置、会话超时时间设置、系统功能模块启用/停用控制、运行规则与阈值参数集中维护 |
| 数据字典配置 | 数据字典分类管理、字典项的增删改查 |
| 数据供给配置 | 基于数据模型自动生成标准API接口、API访问策略配置、API调用鉴权管理 |
| 数据供给监控与统计 | API调用分析、API调用异常告警与日志、API热点分析与调用趋势统计 |
| 身份认证接入配置 | 与公司级认证平台对接配置 |
| 访问控制规则配置 | IP白名单/黑名单规则配置、基于时间段的访问策略设置、白名单规则应用于特定用户或用户组、访问拒绝日志记录与告警、白名单规则批量管理与导入 |
| 模块运行健康监控 | 核心应用服务进程状态监控、数据库连接池状态与性能监控、消息队列深度与堆积告警、服务器资源监控、健康检查汇总视图 |
| 数据备份与恢复 | 备份策略制定与任务调度、系统配置等关键数据备份 |
| 登录日志查询 | 登录日志查询、异常登录行为分析、登录日志统计分析与报表导出 |
| 操作日志查询 | 用户日志查询、操作审计报表生成与合规性报告 |

---

## 设计规范

### 1. 色彩体系
```css
/* 使用 Tailwind 预设色板，主色为蓝色 */
--primary: #3b82f6    /* blue-500 */
--primary-light: #60a5fa  /* blue-400 */
--primary-dark: #2563eb   /* blue-600 */
--bg-card: #1e293b     /* slate-800 */
--bg-surface: #0f172a  /* slate-900 */
--bg-page: #020617     /* slate-950 */
--text-primary: #f8fafc   /* slate-50 */
--text-secondary: #94a3b8 /* slate-400 */
--text-muted: #64748b   /* slate-500 */
--border: #334155     /* slate-700/50 */
--success: #22c55e    /* green-500 */
--warning: #eab308    /* yellow-500 */
--danger: #ef4444     /* red-500 */
--info: #06b6d4       /* cyan-500 */
```

### 2. 组件风格
- 卡片容器：`bg-slate-900 border border-slate-800 rounded-xl`
- 输入框：`bg-slate-800 border border-slate-700 rounded-lg text-white`
- 按钮（主）：`bg-blue-600 hover:bg-blue-700 text-white rounded-lg px-4 py-2`
- 按钮（次）：`bg-slate-800 hover:bg-slate-700 text-slate-300 rounded-lg px-4 py-2`
- 表格：`w-full border-collapse` 表头 `bg-slate-800/50 text-slate-400` 行 `hover:bg-slate-800/30`
- 标签/徽章：`bg-blue-500/20 text-blue-400`、`bg-green-500/20 text-green-400` 等
- 搜索框：左侧搜索图标 + 输入框，`bg-slate-800 border border-slate-700`
- 分页：底部居中，页码按钮 `bg-slate-800 hover:bg-slate-700`

### 3. 页面通用布局
每页布局结构（从上到下）:
```
┌─ 页面标题（二级/三级菜单名称）
├─ 操作栏（搜索框 + 新增按钮 + 批量操作等）
├─ 筛选区（下拉选择器、日期范围等）
├─ 数据表格（列表展示）
├─ 分页（底部）
└─ 弹窗（新增/编辑/详情 Modal，使用 Modal 组件）
```

### 4. 状态管理
- 使用 `useSystem().activeMenu` 获取当前激活菜单
- 菜单路由 ID 格式：`menu-6-x-y`（一级-二级-三级）
- 当前页面用 `{children}` 渲染在 `MainLayout` 中

### 5. 通用功能模式
所有 CRUD 页面通用的操作模式：
- **新增**：右上角「+ 新增」按钮 → 弹出 Modal 表单 → 填写 → 提交 → 刷新列表
- **编辑**：表格行操作列「编辑」按钮 → 弹出 Modal 表单（带初始值）→ 修改 → 提交
- **删除**：表格行操作列「删除」按钮 → Confirm 确认 → 删除 → 刷新列表
- **查询**：顶部搜索框 + 筛选条件 → 点击「查询」→ 更新列表
- **导出**：列表顶部「导出」按钮 → 导出为 Excel/CSV
- **详情查看**：点击行或「详情」按钮 → 弹出详情 Modal 或跳转详情页
- **状态/结果相关页面**：展示任务状态列表、历史记录、审计日志、统计报告等
- **监控/视图页面**：图表可视化（推荐 recharts）、实时数据展示、趋势分析

---

## 功能实现要求

每个三级页面除了基本的数据展示外，还需包含以下交互功能：
- 搜索/筛选（输入框、下拉选择、日期范围）
- 新增/编辑/删除操作（使用 Modal 弹窗）
- 分页加载
- 数据导出
- 加载状态（loading skeleton）
- 空状态展示
- 错误状态处理

### 第1组：运维数据同步配置（4页）

#### 1.1 数据源、目标与映射规则的同步任务配置

**功能说明**：实现运维数据同步配置中的数据源、目标与映射规则的同步任务配置相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 新增配置（Modal表单）
- 编辑配置（Modal表单，带初始值）
- 删除配置（确认弹窗）
- 查询/搜索配置（搜索框 + 筛选条件）
- 列表展示（表格，支持分页）
- 状态管理（启用/停用切换）

#### 1.2 全量与增量同步策略及定时调度

**功能说明**：实现运维数据同步配置中的全量与增量同步策略及定时调度相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 同步任务配置管理
- 同步状态监控
- 同步历史记录
- 手动触发同步

#### 1.3 同步任务状态监控与异常告警

**功能说明**：实时监控运维数据同步配置相关的运行状态、执行进度、异常情况，支持实时刷新

**核心功能**：
- 核心指标数据展示（数字卡片、进度条）
- 趋势图表展示（折线图、柱状图）
- 状态分布统计（饼图、环形图）
- 列表/表格数据展示
- 按条件筛选与搜索
- 数据自动刷新

#### 1.4 同步日志审计

**功能说明**：记录和查询运维数据同步配置相关的操作审计日志，支持按操作人、时间范围检索

**核心功能**：
- 审计日志列表展示（表格）
- 按操作人/时间/类型筛选
- 审计日志详情查看
- 审计报表导出

### 第2组：功能模块权限配置（5页）

#### 2.1 通过建立组织架构树可视化展示与编辑机制

**功能说明**：实现功能模块权限配置中的通过建立组织架构树可视化展示与编辑机制相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 数据列表展示（表格）
- 搜索与筛选
- 新增/编辑/删除操作
- 详情查看

#### 2.2 通过建立角色管理机制

**功能说明**：对功能模块权限配置相关的配置项进行增删改查管理

**核心功能**：
- 新增配置（Modal表单）
- 编辑配置（Modal表单，带初始值）
- 删除配置（确认弹窗）
- 查询/搜索配置（搜索框 + 筛选条件）
- 列表展示（表格，支持分页）
- 状态管理（启用/停用切换）

#### 2.3 通过建立功能菜单与操作按钮权限分配机制

**功能说明**：实现功能模块权限配置中的通过建立功能菜单与操作按钮权限分配机制相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 数据列表展示（表格）
- 搜索与筛选
- 新增/编辑/删除操作
- 详情查看

#### 2.4 通过建立数据范围权限控制机制

**功能说明**：实现功能模块权限配置中的通过建立数据范围权限控制机制相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 数据列表展示（表格）
- 搜索与筛选
- 新增/编辑/删除操作
- 详情查看

#### 2.5 通过建立角色授权关系查询与审计机制

**功能说明**：记录和查询功能模块权限配置相关的操作审计日志，支持按操作人、时间范围检索

**核心功能**：
- 审计日志列表展示（表格）
- 按操作人/时间/类型筛选
- 审计日志详情查看
- 审计报表导出

### 第3组：用户运行授权配置（4页）

#### 3.1 通过建立用户账户信息配置机制

**功能说明**：实现用户运行授权配置中的通过建立用户账户信息配置机制相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 新增配置（Modal表单）
- 编辑配置（Modal表单，带初始值）
- 删除配置（确认弹窗）
- 查询/搜索配置（搜索框 + 筛选条件）
- 列表展示（表格，支持分页）
- 状态管理（启用/停用切换）

#### 3.2 通过建立用户密码安全策略管理机制

**功能说明**：对用户运行授权配置相关的配置项进行增删改查管理

**核心功能**：
- 新增配置（Modal表单）
- 编辑配置（Modal表单，带初始值）
- 删除配置（确认弹窗）
- 查询/搜索配置（搜索框 + 筛选条件）
- 列表展示（表格，支持分页）
- 状态管理（启用/停用切换）

#### 3.3 通过建立用户账户状态管理机制

**功能说明**：对用户运行授权配置相关的配置项进行增删改查管理

**核心功能**：
- 新增配置（Modal表单）
- 编辑配置（Modal表单，带初始值）
- 删除配置（确认弹窗）
- 查询/搜索配置（搜索框 + 筛选条件）
- 列表展示（表格，支持分页）
- 状态管理（启用/停用切换）

#### 3.4 通过建立用户关联组织、角色及信息维护机制

**功能说明**：实现用户运行授权配置中的通过建立用户关联组织、角色及信息维护机制相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 新增配置（Modal表单）
- 编辑配置（Modal表单，带初始值）
- 删除配置（确认弹窗）
- 查询/搜索配置（搜索框 + 筛选条件）
- 列表展示（表格，支持分页）
- 状态管理（启用/停用切换）

### 第4组：运行参数配置（4页）

#### 4.1 系统样式配置

**功能说明**：实现运行参数配置中的系统样式配置相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 新增配置（Modal表单）
- 编辑配置（Modal表单，带初始值）
- 删除配置（确认弹窗）
- 查询/搜索配置（搜索框 + 筛选条件）
- 列表展示（表格，支持分页）
- 状态管理（启用/停用切换）

#### 4.2 会话超时时间设置

**功能说明**：实现运行参数配置中的会话超时时间设置相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 数据列表展示（表格）
- 搜索与筛选
- 新增/编辑/删除操作
- 详情查看

#### 4.3 系统功能模块启用/停用控制

**功能说明**：实现运行参数配置中的系统功能模块启用/停用控制相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 数据列表展示（表格）
- 搜索与筛选
- 新增/编辑/删除操作
- 详情查看

#### 4.4 运行规则与阈值参数集中维护

**功能说明**：实现运行参数配置中的运行规则与阈值参数集中维护相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 新增配置（Modal表单）
- 编辑配置（Modal表单，带初始值）
- 删除配置（确认弹窗）
- 查询/搜索配置（搜索框 + 筛选条件）
- 列表展示（表格，支持分页）
- 状态管理（启用/停用切换）

### 第5组：数据字典配置（2页）

#### 5.1 数据字典分类管理

**功能说明**：对数据字典配置相关的配置项进行增删改查管理

**核心功能**：
- 新增配置（Modal表单）
- 编辑配置（Modal表单，带初始值）
- 删除配置（确认弹窗）
- 查询/搜索配置（搜索框 + 筛选条件）
- 列表展示（表格，支持分页）
- 状态管理（启用/停用切换）

#### 5.2 字典项的增删改查

**功能说明**：实现数据字典配置中的字典项的增删改查相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 数据列表展示（表格）
- 搜索与筛选
- 新增/编辑/删除操作
- 详情查看

### 第6组：数据供给配置（3页）

#### 6.1 基于数据模型自动生成标准API接口

**功能说明**：实现数据供给配置中的基于数据模型自动生成标准API接口相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 数据列表展示（表格）
- 搜索与筛选
- 新增/编辑/删除操作
- 详情查看

#### 6.2 API访问策略配置

**功能说明**：实现数据供给配置中的API访问策略配置相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 新增配置（Modal表单）
- 编辑配置（Modal表单，带初始值）
- 删除配置（确认弹窗）
- 查询/搜索配置（搜索框 + 筛选条件）
- 列表展示（表格，支持分页）
- 状态管理（启用/停用切换）

#### 6.3 API调用鉴权管理

**功能说明**：对数据供给配置相关的配置项进行增删改查管理

**核心功能**：
- 新增配置（Modal表单）
- 编辑配置（Modal表单，带初始值）
- 删除配置（确认弹窗）
- 查询/搜索配置（搜索框 + 筛选条件）
- 列表展示（表格，支持分页）
- 状态管理（启用/停用切换）

### 第7组：数据供给监控与统计（3页）

#### 7.1 API调用分析

**功能说明**：实现数据供给监控与统计中的API调用分析相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 多维度数据分析
- 图表可视化展示
- 分析结果列表
- 分析报告导出

#### 7.2 API调用异常告警与日志

**功能说明**：实现数据供给监控与统计中的API调用异常告警与日志相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 日志列表展示
- 按时间/级别/来源筛选
- 日志详情查看
- 日志导出下载

#### 7.3 API热点分析与调用趋势统计

**功能说明**：实现数据供给监控与统计中的API热点分析与调用趋势统计相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 多维度数据分析
- 图表可视化展示
- 分析结果列表
- 分析报告导出

### 第8组：身份认证接入配置（1页）

#### 8.1 与公司级认证平台对接配置

**功能说明**：实现身份认证接入配置中的与公司级认证平台对接配置相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 新增配置（Modal表单）
- 编辑配置（Modal表单，带初始值）
- 删除配置（确认弹窗）
- 查询/搜索配置（搜索框 + 筛选条件）
- 列表展示（表格，支持分页）
- 状态管理（启用/停用切换）

### 第9组：访问控制规则配置（5页）

#### 9.1 IP白名单/黑名单规则配置

**功能说明**：实现访问控制规则配置中的IP白名单/黑名单规则配置相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 新增配置（Modal表单）
- 编辑配置（Modal表单，带初始值）
- 删除配置（确认弹窗）
- 查询/搜索配置（搜索框 + 筛选条件）
- 列表展示（表格，支持分页）
- 状态管理（启用/停用切换）

#### 9.2 基于时间段的访问策略设置

**功能说明**：实现访问控制规则配置中的基于时间段的访问策略设置相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 数据列表展示（表格）
- 搜索与筛选
- 新增/编辑/删除操作
- 详情查看

#### 9.3 白名单规则应用于特定用户或用户组

**功能说明**：实现访问控制规则配置中的白名单规则应用于特定用户或用户组相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 数据列表展示（表格）
- 搜索与筛选
- 新增/编辑/删除操作
- 详情查看

#### 9.4 访问拒绝日志记录与告警

**功能说明**：实现访问控制规则配置中的访问拒绝日志记录与告警相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 日志列表展示
- 按时间/级别/来源筛选
- 日志详情查看
- 日志导出下载

#### 9.5 白名单规则批量管理与导入

**功能说明**：对访问控制规则配置相关的配置项进行增删改查管理

**核心功能**：
- 新增配置（Modal表单）
- 编辑配置（Modal表单，带初始值）
- 删除配置（确认弹窗）
- 查询/搜索配置（搜索框 + 筛选条件）
- 列表展示（表格，支持分页）
- 状态管理（启用/停用切换）

### 第10组：模块运行健康监控（5页）

#### 10.1 核心应用服务进程状态监控

**功能说明**：实时监控模块运行健康监控相关的运行状态、执行进度、异常情况，支持实时刷新

**核心功能**：
- 核心指标数据展示（数字卡片、进度条）
- 趋势图表展示（折线图、柱状图）
- 状态分布统计（饼图、环形图）
- 列表/表格数据展示
- 按条件筛选与搜索
- 数据自动刷新

#### 10.2 数据库连接池状态与性能监控

**功能说明**：实时监控模块运行健康监控相关的运行状态、执行进度、异常情况，支持实时刷新

**核心功能**：
- 核心指标数据展示（数字卡片、进度条）
- 趋势图表展示（折线图、柱状图）
- 状态分布统计（饼图、环形图）
- 列表/表格数据展示
- 按条件筛选与搜索
- 数据自动刷新

#### 10.3 消息队列深度与堆积告警

**功能说明**：实现模块运行健康监控中的消息队列深度与堆积告警相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 数据列表展示（表格）
- 搜索与筛选
- 新增/编辑/删除操作
- 详情查看

#### 10.4 服务器资源监控

**功能说明**：实时监控模块运行健康监控相关的运行状态、执行进度、异常情况，支持实时刷新

**核心功能**：
- 核心指标数据展示（数字卡片、进度条）
- 趋势图表展示（折线图、柱状图）
- 状态分布统计（饼图、环形图）
- 列表/表格数据展示
- 按条件筛选与搜索
- 数据自动刷新

#### 10.5 健康检查汇总视图

**功能说明**：展示模块运行健康监控相关的综合信息面板，包含数据总览、关键指标统计、操作入口等

**核心功能**：
- 核心指标数据展示（数字卡片、进度条）
- 趋势图表展示（折线图、柱状图）
- 状态分布统计（饼图、环形图）
- 列表/表格数据展示
- 按条件筛选与搜索
- 数据自动刷新
- 操作入口（新建任务、查看详情等快捷按钮）

### 第11组：数据备份与恢复（2页）

#### 11.1 备份策略制定与任务调度

**功能说明**：实现数据备份与恢复中的备份策略制定与任务调度相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 备份策略配置
- 备份任务调度
- 备份文件管理
- 备份状态监控

#### 11.2 系统配置等关键数据备份

**功能说明**：实现数据备份与恢复中的系统配置等关键数据备份相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 新增配置（Modal表单）
- 编辑配置（Modal表单，带初始值）
- 删除配置（确认弹窗）
- 查询/搜索配置（搜索框 + 筛选条件）
- 列表展示（表格，支持分页）
- 状态管理（启用/停用切换）

### 第12组：登录日志查询（3页）

#### 12.1 登录日志查询

**功能说明**：实现登录日志查询中的登录日志查询相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 日志列表展示
- 按时间/级别/来源筛选
- 日志详情查看
- 日志导出下载

#### 12.2 异常登录行为分析

**功能说明**：实现登录日志查询中的异常登录行为分析相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 多维度数据分析
- 图表可视化展示
- 分析结果列表
- 分析报告导出

#### 12.3 登录日志统计分析与报表导出

**功能说明**：实现登录日志查询中的登录日志统计分析与报表导出相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 多维度数据分析
- 图表可视化展示
- 分析结果列表
- 分析报告导出

### 第13组：操作日志查询（2页）

#### 13.1 用户日志查询

**功能说明**：实现操作日志查询中的用户日志查询相关功能，支持数据的查询、展示和管理操作

**核心功能**：
- 日志列表展示
- 按时间/级别/来源筛选
- 日志详情查看
- 日志导出下载

#### 13.2 操作审计报表生成与合规性报告

**功能说明**：记录和查询操作日志查询相关的操作审计日志，支持按操作人、时间范围检索

**核心功能**：
- 审计日志列表展示（表格）
- 按操作人/时间/类型筛选
- 审计日志详情查看
- 审计报表导出

## 技术要求

1. 使用 **Next.js App Router** 模式（`src/app/` 目录）
2. 全部使用 **'use client'** 组件
3. 使用 **TailwindCSS** 样式（使用预设的深色主题色板）
4. 图标使用 **lucide-react**
5. 表格、表单、弹窗等通用组件如果已有则复用，否则自行实现
6. 图表推荐使用 **recharts** 库
7. 所有代码保持中文注释
8. 遵循现有项目的文件命名和代码风格约定

---

## 产出文件结构参考

```
src/
  pages/module6/                    ← 模块6所有页面组件
    dataSyncConfig/                   ← 第1组 运维数据同步配置
      SyncTaskConfig.tsx
      SyncStrategySchedule.tsx
      SyncStatusMonitor.tsx
      SyncLogAudit.tsx
    modulePermissionConfig/                   ← 第2组 功能模块权限配置
      Page6390.tsx
      Page6883.tsx
      Page5682.tsx
      Page121.tsx
      Page1585.tsx
    userAuthConfig/                   ← 第3组 用户运行授权配置
      Page3383.tsx
      Page9952.tsx
      Page6961.tsx
      Page1837.tsx
    runParamConfig/                   ← 第4组 运行参数配置
      Page4789.tsx
      SessionTimeoutConfig.tsx
      Page6431.tsx
      GlobalParamConfig.tsx
    dataDictConfig/                   ← 第5组 数据字典配置
      DataDictCategory.tsx
      DataDictItems.tsx
    dataSupplyConfig/                   ← 第6组 数据供给配置
      ApiAutoGenerate.tsx
      ApiAccessPolicy.tsx
      ApiAuthManagement.tsx
    dataSupplyMonitor/                   ← 第7组 数据供给监控与统计
      ApiCallAnalysis.tsx
      ApiExceptionAlert.tsx
      ApiHotspotTrend.tsx
    identityAuthConfig/                   ← 第8组 身份认证接入配置
      IdentityAuthConfig.tsx
    accessControlConfig/                   ← 第9组 访问控制规则配置
      IpWhitelistBlacklist.tsx
      TimeBasedAccessPolicy.tsx
      UserGroupWhitelist.tsx
      AccessDenyLogAlert.tsx
      WhitelistBatchImport.tsx
    healthMonitor/                   ← 第10组 模块运行健康监控
      ServiceProcessMonitor.tsx
      DatabasePoolMonitor.tsx
      MessageQueueMonitor.tsx
      ServerResourceMonitor.tsx
      HealthCheckSummary.tsx
    dataBackupRestore/                   ← 第11组 数据备份与恢复
      BackupPolicySchedule.tsx
      KeyDataBackup.tsx
    loginLogQuery/                   ← 第12组 登录日志查询
      LoginLogQuery.tsx
      AbnormalLoginAnalysis.tsx
      LoginLogStatistics.tsx
    operationLogQuery/                   ← 第13组 操作日志查询
      UserOperationLogQuery.tsx
      OperationAuditReport.tsx
```
